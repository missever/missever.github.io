vector<int> base;

void add(int x) {
    for(auto u:base) x = min(x, u ^ x);
    if(!x) return;
    for(auto &u:base) u = min(u, u ^ x);
    base.push_back(x);
}

int query(int x) {
    for(auto u:base) x = min(x, u ^ x); // x = max(x, u ^ x);
    return x;
}