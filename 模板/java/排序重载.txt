//创建一个该类的List
List<Integer> h = new ArrayList<Integer>();
//List 赋值
for(int i = 0;i < n; i++) h.add(i);
//排序重载 相等返回0，小于返回-1，大于返回1
Collections.sort(h,new Comparator<Integer>() {
    public int compare(Integer a,Integer b){
	if(R[a] == R[b]) return 0;
	else if(R[a] < R[b]) return -1;
	else return 1;
}
});